import Foundation

/// Copy this file to `Secrets.swift` (gitignored) in the same folder and
/// fill in your own key. `Secrets.swift.template` itself is not compiled —
/// its extension is deliberately not `.swift` alone so Xcode's
/// file-system-synchronized group doesn't pick it up as a second source
/// defining the same type.
enum Secrets {
    static let geminiAPIKey = "YOUR_GEMINI_API_KEY"
}
